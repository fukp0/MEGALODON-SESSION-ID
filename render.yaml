services:
  - type: web
    name: megalodon
    env: docker
    plan: free
    branch: main
    repo: https://github.com/townen2/meg-lodon-session
    dockerfilePath: ./Dockerfile
    autoDeploy: true
